{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "HTTP Server 8000",
            "type": "debugpy",
            "request": "launch",
            "module": "http.server",
            "args": ["8000"],
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}